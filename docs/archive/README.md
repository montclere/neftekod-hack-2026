# Архив

Справочники. В работе не нужны, удалять жалко.

| Файл | Что это |
|---|---|
| `SETUP-full.md` | как этот репозиторий собирался с нуля: `uv init`, `create-next-app`, `shadcn init`. Пригодится на следующем хакатоне, если начинать заново. **Повторять сейчас не нужно — всё уже собрано,** для клона достаточно `make setup` из корневого README |
| `STACK.md` | развёрнутое обоснование выбора стека, слой за слоем. Короткая версия — в [`../adr/0002-stack.md`](../adr/0002-stack.md) |

Оба варианта витринного README живут не здесь, а в [`../pitch/`](../pitch/) — они нужны в работе, на фазе упаковки.

Часть команд и путей в этих файлах устарела относительно текущего репозитория.
Источник правды — корневой `README.md` и `AGENTS.md`.
